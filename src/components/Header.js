const Header = {
  render: () => {
    const view = `
            <header class="header">
                <h1> Make your things easier </h1>
            </header>
        `;
    return view;
  },
};

export default Header;
